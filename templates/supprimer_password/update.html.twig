{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié - La Marque 42{% endblock %}

{% block content %}
    <div class="block-small">

        {% for message in app.flashes('notice') %}
            <div class="alert-info">{{ message }}</div>
        {% endfor %}
      {{ form(form) }}
    </div>
{% endblock %}
