{% extends 'base.html.twig' %}

{% block title %}Mon Adresses - La Marque 42 {% endblock %}

{% block content %}

    <a href="{{ path('compte_adresse-ajouter') }}" class="btn btn-secondary btn-md float-right ">Ajouter une adresse</a>
    <h1>Mes adresses</h1>

    C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses. <br>

    <a href="{{ path('compte') }}" class="float-right">Retour</a><br>
    <hr>
    {# {{ dump(app.user) }} #}
    {% if app.user.adresses |length == 0 %}
       <p class="text-center">Vous n'avez pas encore ajouté d'adresse dans votre compte client.
           Pour en ajouter une, veuillez <a href="">Cliquez-ici</a>
       </p>
        {% else %}
        Afficher les adresses
    {% endif %}

{% endblock %}
{# #}
